/* 编写C程序,计算并输出下面分段函数的值(x值由键盘输入)
   x²+x-6   x<0且x≠-3
y= x²-5x+6  0≤x<10且x≠2及x≠3
   x²-x-1   其他
运行程序并分别输入以下测试数据,验证程序的正确性
    除-3.0以外的任意负数(x²+x-6)
  -3.0(11.0)
  0.0(6.0)
  2.0(1.0)
  3.0(5.0)
  5.0(6.0)
    10.0(89.0)
  11.0(109.0)                                        */
#include <stdio.h>

main()
{
    float x;

    printf("Input x:");
    scanf("%f", &x);
    if (x < 0 && x + 3)
        printf("y=%.1f\n", (x + 3) * (x - 2));
    else if (x >= 0 && x < 10 && (x - 3) * (x - 2))
        printf("y=%.1f\n", (x - 3) * (x - 2));
    else
        printf("y=%.1f\n", x * x - x - 1);
}