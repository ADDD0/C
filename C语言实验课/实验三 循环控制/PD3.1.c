/* 编程求下列级数的和:
s=1/(1×2)+1/(2×3)+1/(3×4)+1/(4×5)+……+1/(n×(n+1))
具体要求:
①最后一项小于0.001时则停止计算
②输出形式为
n=具体值,s=级数和的具体值                        */
#include <stdio.h>

main()
{
    int n, i;
    float s, j;

    scanf("%d", &n);
    for (s = 0, i = j = 1; j >= 0.001 && i <= n; ++i) {
        j = 1.0 / i / (i + 1);
        s += j;
    }
    printf("%.3f\n", s);
}