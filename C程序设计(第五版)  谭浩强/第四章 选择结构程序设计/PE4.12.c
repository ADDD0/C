#include <stdio.h>

/* 有4个圆塔,圆心分别为(2,2),(-2,2),(-2,-2),(2,-2),
圆半径为1.这4个塔的高度为10m,塔以外无建筑物.今输入
任一点的坐标,求该点的建筑高度(塔外的高度为零) */
main()
{
    float x, y;

    scanf("%f,%f", &x, &y);
    if ((x - 2) * (x - 2) + (y - 2) * (y - 2) <= 1 ||
    (x + 2) * (x + 2) + (y - 2) * (y - 2) <= 1 ||
    (x + 2) * (x + 2) + (y + 2) * (y + 2) <= 1 ||
    (x - 2) * (x - 2) + (y + 2) * (y + 2) <= 1)
        printf("height = 10m");
    else
        printf("height = 0m");

}